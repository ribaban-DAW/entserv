{% extends 'base.html.twig' %}

{% block title %}Pintura{% endblock %}

{% block body %}
    <h1>Pintura</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pintura.id }}</td>
            </tr>
            <tr>
                <th>Código</th>
                <td>{{ pintura.codigo }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ pintura.color }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ pintura.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary my-2" href="{{ path('app_pintura_index') }}">Volver</a>

    <a class="btn btn-primary" href="{{ path('app_pintura_edit', {'id': pintura.id}) }}">Editar</a>

    {{ include('pintura/_delete_form.html.twig') }}
{% endblock %}
