<form method="post" action="{{ path('app_piedra_delete', {'id': piedra.id}) }}" onsubmit="return confirm('¿Desea eliminar elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piedra.id) }}">
    <button class="btn btn-danger">{{ button_label|default('Eliminar') }}</button>
</form>
