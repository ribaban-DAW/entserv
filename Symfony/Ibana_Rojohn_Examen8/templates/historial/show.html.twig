{% extends 'base.html.twig' %}

{% block title %}Historial{% endblock %}

{% block body %}
    <h1>Historial</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ historial.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ historial.Fecha ? historial.Fecha|date('d-m-o H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Paciente</th>
                <td>{{ historial.Paciente }}</td>
            </tr>
            <tr>
                <th>CodigoHistorial</th>
                <td>{{ historial.CodigoHistorial }}</td>
            </tr>
            <tr>
                <th>Especialista</th>
                <td>{{ historial.Especialista }}</td>
            </tr>
            <tr>
                <th>Alergias</th>
                <td>{{ historial.Alergias }}</td>
            </tr>
            <tr>
                <th>Antecedentes</th>
                <td>{{ historial.Antecedentes }}</td>
            </tr>
            <tr>
                <th>Sintomas</th>
                <td>{{ historial.Sintomas }}</td>
            </tr>
            <tr>
                <th>Diagnostico</th>
                <td>{{ historial.Diagnostico }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary my-2" href="{{ path('app_historial_index') }}">Volver</a>

    <a class="btn btn-warning" href="{{ path('app_historial_edit', {'id': historial.id}) }}">Editar</a>

    {{ include('historial/_delete_form.html.twig') }}
{% endblock %}
