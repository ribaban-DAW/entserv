{% extends 'base.html.twig' %}

{% block title %}Tipo{% endblock %}

{% block body %}
    <h1>Tipo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary my-2" href="{{ path('app_tipo_index') }}">Volver</a>

    <a class="btn btn-primary" href="{{ path('app_tipo_edit', {'id': tipo.id}) }}">Editar</a>

    {{ include('tipo/_delete_form.html.twig') }}
{% endblock %}
